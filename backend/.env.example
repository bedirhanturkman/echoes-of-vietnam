# OpenAI API Key (required for embedding generation)
OPENAI_API_KEY=sk-your-key-here

# Set to "true" to use mock embeddings (no API key needed)
USE_MOCK_EMBEDDINGS=true

# Musical defaults
DEFAULT_SCALE=C_major_pentatonic
DEFAULT_TEMPO=72
DEFAULT_PROGRESSION=knockin_verse
